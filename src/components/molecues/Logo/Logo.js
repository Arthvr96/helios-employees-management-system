import React from 'react';
import { LogoWrapper, Title } from './Logo.style';

const Logo = () => {
  return (
    <LogoWrapper>
      <Title>Helios</Title>
    </LogoWrapper>
  );
};

export default Logo;
